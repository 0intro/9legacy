#!/bin/rc
clock=`{cat /dev/time}
xx=`{{echo $clock(5) / '(10^9)'; echo '_/86400'}|hoc|sed 's/^\./0./g;s/\..*//g'}
sec=$xx(1)
days=$xx(2)
cat /dev/sysname
echo -n ' up '$days' days, '
timezone='MDT -21600' date $sec |awk '{print $4}'
